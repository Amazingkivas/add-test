# Implementation and testing of the add function

## 0. Download all submodules
  ```
  git submodule update --init --recursive
  ```

## 1. Build the project
  ```
  mkdir build
  cd build
  cmake ..
  cmake --build . --config RELEASE
  ```